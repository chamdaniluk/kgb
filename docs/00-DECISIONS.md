# Log Keputusan (Decision Log) — SI CENDIKIA

Catatan keputusan penting. Format: tanggal, keputusan, alasan, konsekuensi.

| Tanggal | Keputusan | Alasan | Konsekuensi |
|---------|-----------|--------|-------------|
| 2026-08-16 | Proyek baru bernama **SI CENDIKIA** (Sistem Cepat Efektif Non-stop Digital Informasi Kenaikan Gaji Berkala ASN), terpisah dari sistem live e-KGB | Owner ingin sistem yang lebih sederhana dan lebih ringan; hasil akhir = terbitnya surat/SK KGB | Sistem live `/var/www/ekgb` hanya referensi domain, read-only |
| 2026-08-16 | Cakupan pengguna: ASN (PNS dan PPPK) lingkungan Dinas Pendidikan; **piloting tahap awal khusus guru** | Permintaan owner | Master data & role awal fokus guru; struktur tetap memungkinkan perluasan ke tenaga kependidikan lain |
| 2026-08-16 | Login ASN: **NIP sebagai username, NIP sebagai password** | Agar mudah bagi pengguna | Dicatat sebagai risiko keamanan di PRD; menunggu keputusan mitigasi (lihat open question PRD) |
| 2026-08-16 | Berkas unggahan: **PDF, maksimal 5MB per file** | Permintaan owner | Validasi tipe & ukuran di sisi server dan klien |
| 2026-08-16 | Alur verifikasi: **berjenjang** — unit (Korwil/SMP/SKB sesuai tempat bekerja) → Dinas → **TTE pimpinan** → surat terbit (PDF) | Permintaan owner | Setiap transisi status diaudit |
| 2026-08-16 | Toolchain agent: Context7, codebase-memory MCP, Memory MCP, Spec Kit, Superpowers, Ponytail, Caveman, RTK | Konsistensi dengan proyek CMS Sekolahku | Semua agent wajib memakai tool ini (lihat AGENTS.md) |
| 2026-08-16 | Keamanan kredensial via hook PreToolUse + AGENTS.md + .gitignore, termasuk larangan baca `/var/www/ekgb/env` dan `deploy/` | Perlapisan: deterministik + imbauan + pencegahan commit | Agent tidak bisa membaca file kredensial apa pun |
| 2026-08-16 | **PRD v1.0 FINAL** — 5 open question diputuskan owner | Keputusan langsung owner | Fase DATABASE ERD dibuka |
| 2026-08-16 | Login ASN: NIP sebagai username DAN NIP sebagai password, berlaku seterusnya | Keputusan owner (risiko diterima, mitigasi rate-limit + hashing) | Tidak ada fitur ganti password ASN di cakupan awal |
| 2026-08-16 | Berkas pengajuan: **1 file PDF saja**, maks 5MB | Keputusan owner (penyederhanaan dari e-KGB yang memakai 2–3 berkas) | Skema cukup satu entitas lampiran per pengajuan |
| 2026-08-16 | TTE memakai sertifikat elektronik **BSrE/BSSN** (kedepannya) | Keputusan owner | Arsitektur harus mengakomodasi integrasi layanan TTE BSrE |
| 2026-08-16 | Master data ASN dari **file BKN milik Dinas Pendidikan** (impor) | Keputusan owner | Perlu fitur impor file BKN oleh admin; tanpa integrasi live ke Dapodik/BKN |
| 2026-08-16 | Format surat mengikuti **template e-KGB** | Keputusan owner (acuan resmi) | Template divariabelkan: nomor, nama, NIP, unit, TMT, gaji lama→baru, tanggal terbit |
| 2026-08-16 | Impor BKN **hanya sekali di awal** (seeding); pemutakhiran data lewat pengajuan perubahan guru + verifikasi Dinas dengan bukti berkas | Keputusan owner | PRD v1.1 menambah F-22..F-27; ERD v0.2 menambah tabel `teacher_changes` |
| 2026-08-16 | **Penyederhanaan**: perubahan data guru HANYA lewat pengajuan KGB, sekalian bukti dukung & kelengkapan berkas; mekanisme perubahan terpisah dihapus | Keputusan owner | PRD v1.2 (F-22..F-24); ERD v0.3 kembali 8 tabel (`teacher_changes` dihapus); master data diperbarui otomatis saat surat terbit |
| 2026-08-16 | Submit ulang setelah ditolak: **ditolak Korwil/unit → ulang dari unit; ditolak Dinas → langsung dari Dinas** | Keputusan owner | Mesin status ERD v0.4 diperbaiki; cukup 1 kolom `status` + audit_logs |
| 2026-08-16 | Nomor surat **otomatis dari template yang dapat diisi Dinas** | Keputusan owner | ERD v0.4 menambah tabel `letter_number_templates` (token {SEQ}/{YEAR}) |
| 2026-08-16 | Data gaji **tidak ada di file BKN**; dihitung dari **tabel skala gaji PNS & PPPK terbaru** (PP 5/2024 & Perpres 11/2024) sesuai masa kerja + pangkat/golongan; PPPK guru golongan **tetap IX** | Keputusan owner | ERD v0.4: kolom `gaji_pokok` dihapus dari `teachers`, diganti `masa_kerja_tahun`; `salary_scales` wajib (asn_type + golongan + masa kerja); PRD v1.3 (F-5, F-9a) |
| 2026-08-16 | Penegasan owner: gaji PNS ditentukan **pangkat/golongan DAN masa kerja** — tidak boleh disamakan dengan logika PPPK (golongan tetap IX) | Klarifikasi owner | ERD FINAL menambah §7a logika pencarian gaji; lookup gagal = tolak, bukan tebak |
| 2026-08-16 | Kolom tahun periode: **tidak perlu**; **ERD v1.0 FINAL** disetujui owner | Persetujuan owner | Fase 3 API DOCUMENTATION dibuka |
| 2026-08-16 | **Beranda publik** dengan grafik informasi SI CENDIKIA (statistik agregat), menu **Tatacara Penggunaan**, dan menu **Alur Pengajuan** | Permintaan owner | PRD v1.4 (F-28..F-30); API v1.1 menambah `GET /public/stats`; UI/UX v0.2 menambah beranda + 2 halaman statis |
| 2026-08-16 | **Akun petugas resmi**: 99 akun (1 TTE, 6 Dinas, 19 Korwil, 73 SMP/SKB) dari file `daftar-akun-petugas.xlsx`. **Username petugas TIDAK boleh sama dengan NIP** (anti-bentrok). File berisi password → disimpan di `local/` (git-ignored) | Keputusan owner | PRD v1.5 (F-2, keputusan #9); ERD `users` diperbarui; aturan validasi username unik & bukan NIP masuk fase implementasi |
| 2026-08-16 | **Password petugas: plain dulu** — password awal langsung berlaku, tanpa paksa ganti saat login pertama (tetap di-hash saat disimpan) | Keputusan owner | ERD `users` diperbarui; risiko kecil diterima, mitigasi rate-limit + audit login |
| 2026-08-16 | **TTE memakai eSign Kominfo (BSrE/BSSN)** — API resmi "Esign Client Service for User 2.2.2" (arsip Postman collection + contoh PDF dari Kominfo); mode VISIBLE, NIK + passphrase pimpinan; env dev `esign-dev.layanan.go.id` | Keputusan owner ("TTE pakai ini" + kirim arsip) | PRD v1.6 (F-15, §10); API v1.2; ARCHITECTURE v1.1; TECH STACK v0.2; arsip disimpan `local/esign-kominfo/` (git-ignored, berisi kredensial uji) |
| 2026-08-16 | **TECH STACK v1.0 FINAL**: PDF = `wkhtmltopdf`; frontend = server-rendered + vanilla JS; Go stdlib + pgx + PostgreSQL 16 | Persetujuan owner atas rekomendasi | Keenam artefak selesai; implementasi dapat dimulai. **Direvisi 2026-08-17**: renderer HTML/`wkhtmltopdf` diganti template DOCX dinas + LibreOffice `soffice` (lihat keputusan 2026-08-17 tentang template DOCX) |
| 2026-08-16 | **ARCHITECTURE v1.2** — harmonisasi editorial: diagram §2/§4 dan tabel risiko §10 yang masih menulis "TTE BSrE (kedepannya)/simulasi" diselaraskan dengan §6 (eSign Kominfo, integrasi nyata) | Sisa redaksi lupa ter-update saat v1.0 → v1.1; dokumen adalah rujukan implementasi | Tanpa perubahan desain; kini seluruh dokumen konsisten mengacu eSign Kominfo |
| 2026-08-17 | SK KGB diisi dari **template DOCX dinas** (bukan HTML); form usulan menampilkan seluruh field naskah; naskah bolong tidak boleh submit/setuju/TTE | Keputusan owner setelah analisis output tidak sesuai contoh | Renderer HTML dihapus; gerbang `LETTER_DRAFT_INCOMPLETE`; NIP pimpinan terpisah dari NIK eSign |
| 2026-08-17 | KGB berkala PNS/PPPK **dua tahun sekali, TMT mengikuti anniversary SK terakhir, masa kerja selalu genap 0 bulan**. Tanggal TTD = hari penetapan, bukan TMT berlaku | Koreksi owner: usul Agustus, SK pertama 1 Des 2020 → berlaku 1 Des 2026, MKG 6 th 0 bln | Server menghitung TMT/MKG; isian bebas ditolak |
| 2026-08-25 | **Cutover produksi**: SI CENDIKIA dipasang dan dijalankan pada domain live `kgb.grobogankab.web.id` oleh owner | Persetujuan cutover sebelumnya (checklist deploy) telah dieksekusi owner | Sistem berstatus LIVE; source e-KGB `/var/www/ekgb` tetap hanya referensi read-only; fokus berikutnya: operasional (backup/restore drill, monitoring, sisa checklist go-live) |
| 2026-08-25 | **Seragam satu rumus jadwal KGB** (`letterdata.NextDueTMT`, dipakai alur usulan, pratinjau gaji, dasbor `/me`, dan laporan nominasi): TMT = anniversary dua tahunan pertama dari TMT awal yang lebih baru dari SK terakhir; **usulan telat tidak pernah melompat siklus** (lupa usul sampai Jan 2027 → TMT SK tetap 1 Des 2026; gaji nominal baru dinikmati ±1 bulan setelah proses, urusan pembayaran di luar aplikasi) | Koreksi owner atas perilaku lama yang melompat ke 2028 + perintah "harusnya seragam karena menggunakan 1 hitungan"; meluruskan entri 2026-08-17 ("anniversary SK terakhir") | Fungsi lama `applyPeriodicSchedule`/`ComputePeriodicMasaKerja`/`IsPeriodicTMT` dihapus; baris tanpa `tmt_awal` kini dilaporkan `belum_lengkap` pada nominasi; tagihan tertinggal ≥2 tahun bergeser otomatis ke anniversary berjalan |
| 2026-08-25 | **Cap gaji di puncak skala**: bila masa kerja melewati bracket tertinggi tabel golongan itu (mis. 36 th pada skala yang berhenti di 32), gaji pratinjau/usulan mengikuti nilai tertinggi tabel — gaji berikutnya sama dengan gaji sekarang (berhenti naik di puncak); pangkat/golongan/jabatan tetap milik guru, tidak diubah sistem. Batas bawah tetap gagal keras (ERD §7a) | Instruksi owner: "mengikuti tabel gaji tertinggi sesuaikan pangkat golongan jabatan apabila PNS" | `SalaryCurrentNext` menambah fallback bracket tertinggi untuk `next`; test `TestSalaryCurrentNextCapsAtTopBracket` |
| 2026-09-03 | **Sinkron ASN dari SIPP ASN** (`GET sippasn.grobogan.go.id/api/pegawai`, endpoint publik daftar pejabat, tanpa kredensial): hanya jabatan fungsional guru aktif di unit Dinas Pendidikan yang di-upsert ke master (kunci NIP, idempoten); golongan kosong + NIP 2025 = PPPK baru (IX), golongan kosong lainnya dilewat; baris KGB-terbit tidak ditimpa | Permintaan owner: REST API sinkron data ASN; login-pegawai SIPP ASN ber-captcha sehingga tidak dapat dipakai untuk sinkron, sedangkan endpoint publik hanya berisi daftar pejabat (bukan master ASN penuh) | Endpoint `POST /admin/sync-sippasn`, `POST /admin/sync-sippasn/preview` (read-only), `GET /admin/sync-sippasn/history`; migrasi 011 (`bkn_imports.asal_data`); audit `sinkron_sippasn`; env `SIPPASN_BASE_URL`/`SIPPASN_TIMEOUT_SECONDS` |
| 2026-09-03 | **SIPPASN = sumber utama data induk; sinkron malam VPS + refresh saat login; cakupan seluruh pegawai Disdik (guru + non-guru)**. Sinkron tiap malam (keputusan owner), SIPPASN menang untuk nama/jenis/kategori/unit/golongan/jabatan, SK milik pegawai penentu gaji. Jenis ASN dari segmen NIP (01–12 = PNS, 21–22 = PPPK, terkonfirmasi di 15.463 baris). PPPK non-guru memakai golongan SIPPASN (II/a–III/d), bukan IX. Admin read-only atas data ASN (approval saja) | Owner: data harus selalu update mengikuti SIPPASN (data utama pegawai); ke depan non-guru ikut usul KGB; menggantikan keputusan "impor BKN sekali di awal" | Migrasi 012 (`teachers.kategori`); `SIPPASNSnapshot` cache TTL 6 jam (stale-while-revalidate); scheduler malam + `RefreshFromSIPPASN` saat login; validasi jabatan/golongan per kategori di form KGB + UI; audit `sinkron_sippasn_malam`/`sinkron_sippasn_login`; env `SIPPASN_SNAPSHOT_TTL`/`SIPPASN_NIGHTLY_*` |
| 2026-09-03 | **Isian KP Terakhir dan KGB Terakhir dipisah (tidak disatukan)**. Bila ada KP terbaru, gaji KGB mengacu golongan KP dengan jangka waktu tetap 2 tahun dari KGB terakhir; tanpa KP, golongan sama + MKG dari KGB terakhir. Contoh: CPNS Des 2020, KP IIIa→IIIb, usul KGB Des 2026 → gaji IIIb MKG 6 | Owner + contoh kasus: KP Juli di tengah siklus mengubah tabel gaji tanpa menggeser jadwal KGB | Migrasi 013 (kolom KP di `teachers` + `submissions`); `store.KPLast` + `EffectiveGolongan`; `parseKPLast` (KP sebagian → 422); salary-preview param KP + `golongan_efektif`; UI dua seksi; master diperbarui KP+KGB saat terbit |
| 2026-09-03 | **Revisi seksi KP/KGB**: tiap seksi punya Masa Kerja (th/bl) sesuai SK; blok bawah naskah SK (SK terakhir + MKG lama/baru) dihapus. Golongan KP dikunci SIPPASN; Golongan KGB = golongan ruang saat KGB terakhir, dapat diubah. Naskah SK (pangkat/golongan + jabatan) mengikuti KP terakhir, tanpa input ganda | Owner: MKG terlihat dari SK terbit; KP kunci, KGB editable; naskah ikut KP | Migrasi 014 (MKG KP/KGB di `submissions` + `teachers`); `KGBLast`; `parseKGBLast`; `letterDraftFromForm` tanpa field bawah; UI seksi + hapus blok; test kunci KP 422 |
| 2026-09-03 | **MKG KGB = MKG KGB sebelumnya + 2 (peninjauan masa kerja wiyata bakti)**. Contoh: CPNS Des 2020 + peninjauan 4 th → SK sebelumnya MKG 8 → KGB 2026 MKG 10 (bukan 6 dari TMT). Tanpa MKG tersimpan: fallback hitung TMT | Owner: SK sebelumnya pasti benar karena sudah terbit terverifikasi | `letterdata.MasaKerjaKGB`; submit + preview pakai aturan sama (param `last_kgb_masa_tahun`); UI kirim MKG SK; test unit + handler |
| 2026-09-04 | **Bersih produksi lokal**: data percobaan dihapus (2 pengajuan + 3 akun demo + seluruh audit + PDF); tidak ada kata demo/panchole di UI; **seluruh kolom wajib** — seksi KP (7 kolom), seksi KGB, naskah SK, dan perpanjangan PPPK; tanpa KP → 422 | Owner: siap produksi | TRUNCATE audit/submissions demo; UI required + label tanpa "opsional"; `parseKPLast` + `ValidateDraft` mewajibkan KP; fixture test diperbarui |
| 2026-09-04 | **Deploy VPS rilis sinkron SIPPASN**: binary baru + migrasi 011–014, env SIPPASN (nightly 24h); sinkron awal 15.463 baris → 1.616 baru + 7.281 update (teachers 7.301 → 8.917); DB lokal tidak di-restore (35 usulan produksi VPS dipertahankan) | Perintah owner deploy + sinkron agar data selalu update dari SIPPASN | Backup DB VPS `/tmp/si_cendikia-pre-deploy-20260904.dump` + binary `.bak.20260904`; akun bootstrap sementara dinonaktifkan pasca-sinkron |
| 2026-09-04 | **Hapus 35 usulan ujicoba di VPS** (letters + submissions + 38 PDF + nul-kan 138 audit ref); riwayat audit aksi dipertahankan | Owner: data produksi hanya ujicoba, belum digunakan | VPS bersih: submissions 0, letters 0; backup pra-hapus ada di dump pre-deploy |
| 2026-09-04 | **Banner uji coba internal + hitung mundur 7 hari di beranda** (bahasa kedinasan resmi; default 7 hari dari start, dapat dikunci via `TRIAL_UNTIL`) | Perintah owner: tandai layanan sedang ujicoba internal | `trialNoticeTarget` + CSS `.trial-notice`/`.trial-countdown`; test `trial_notice_test.go`; env example `TRIAL_UNTIL` |
| 2026-09-04 | **Saklar banner uji coba lewat Dasbor Admin** (kartu "Masa uji coba internal": checkbox tampil + input tanggal + Simpan; endpoint `GET/PUT /api/v1/admin/trial-notice`; tabel `app_settings` migrasi 015; DB menang, env `TRIAL_UNTIL` cadangan, default 7 hari; audit `ubah_pengumuman_uji_coba`) | Owner: apakah bisa dimatikan lewat admin utama | Beranda fail-open (DB gagal → env/default, tidak 500); UI hanya peran `admin`, endpoint pola lama (`admin`+`admin_dinas`) |
| 2026-09-04 | **Dasbor admin: 4 tab akun (Admin/Guru/Unit/Dinas) + filter unit+kecamatan+tipe + log aksi/tanggal** (endpoint `users?role_group/unit_id/kecamatan/unit_type`, `teachers?...`, `audit-logs?actor/from/to`, `filter-options`, `audit-logs/actions`; kolom `role_group`/`unit_type`; kecamatan dari relasi korwil `KORWILCAM <X>`; UI tab + badge jumlah + 3 dropdown + filter log) | Owner: admin utama mengatur akun & log; pisahkan admin-user; kelompokkan SD/SMP/Dinas/kecamatan/unit | Tanpa migrasi (kolom sudah ada); endpoint pola lama (`admin`+`admin_dinas`), UI tab hanya peran `admin` |
| 2026-09-07 | **Alur jenjang per kecamatan + usulan Dinas langsung**: setiap usulan membawa jenjang (TK/SD/SMP/SKB/Korwil/Dinas), kecamatan, dan unit kerja; TK/SD diverifikasi Korwil sekecamatan (relasi parent atau kolom kecamatan), SMP/SKB oleh admin unitnya sendiri, **usulan pegawai unit Dinas langsung berstatus `menunggu_dinas`**; **admin Dinas melihat semua usulan tetapi hanya dapat approve/reject usulan pegawai Dinas** (jenjang lain read-only, diproses verifikator Dinas) | Laporan owner: ada usulan masuk tetapi admin Dinas tidak melihatnya — akar: usulan `menunggu_unit` tak tercakup scope (772 SD tanpa parent Korwil; unit Dinas tanpa jalur verifikasi) + antrean Dinas hanya `menunggu_dinas` | Migrasi 016 (`units.district` + backfill kecamatan Korwil); `ListQueue`/`UnitInScope` berbasis kecamatan; `CreateSubmission` status awal per jenjang; `handleDinasDecision` menolak admin_dinas untuk non-Dinas (403); kolom `unit_type`/`unit_district`/`unit_korwil_name` di antrean/detail/nominasi; endpoint `PATCH /admin/units/{id}` melengkapi kecamatan+parent |
| 2026-09-09 | **Admin Dinas memverifikasi SEMUA usulan dari unit** (revisi keputusan 2026-09-07): admin.disdik1-6 dapat approve/reject usulan TK/SD/SMP/SKB maupun pegawai Dinas; blokir 403 + mode "Hanya lihat" dicabut di server dan UI | Laporan owner: setelah ACC unit, dinas tak bisa ACC ("Akun Admin Dinas hanya memproses usulan pegawai Dinas") padahal kursi verifikator_dinas kosong; klarifikasi owner: pegawai Dinas langsung ke Dinas tanpa tahap unit, unit tetap berjenjang, admin dinas 1-6 verifikasi semua usulan masuk | `handleDinasDecision` tanpa batasan peran; `canDecide=true` di review dinas; test `TestAdminDinasProsesSemuaUsulan` |
| 2026-09-09 | **Naskah SK mengikuti SK terbaru berdasarkan TANGGAL SK** (revisi aturan KP/KGB): bandingkan tanggal SK KP (PNS) / SK Pertama-Perpanjangan Kontrak (PPPK) vs tanggal SK KGB; pemenang mengisi seluruh identitas SK terakhir naskah (pejabat, tanggal, nomor, TMT). MKG lama tetap dari SK KGB; golongan efektif + salary-preview + TTE selaras memakai tanggal SK | Koreksi owner: kolom "SK terakhir" sebelumnya dari SK KGB apa pun tanggalnya; yang benar satu SK terbaru (KP/KGB/Perpanjangan) | `store.SKNewer` + `EffectiveGolongan` basis tanggal SK; `prepareKGBFromForm` menimpa `LastSK*` dari KP bila menang; test `TestSKNewer`/`TestEffectiveGolonganTanggalSK` |
| 2026-09-09 | **MKG lama naskah + gaji lama mengikuti masa kerja SK pemenang** (konsistensi satu turunan SK): bila SK KP menang, MKG lama = masa SK KP dinormalisasi ke grid berkala (genap, 0 bulan; mis. 5 th 7 bl → 4 th 0 bl), MKG baru = lama + 2, gaji lama/baru dari bracket tersebut; masa asli SK tetap tersimpan di kolom last_kp_*. Salary-preview + refreshSalary diselaraskan (kirim masa+tanggal kedua SK) | Koreksi owner: baris "Masa kerja golongan pada tgl. tsb" (poin 6 SK KGB PNS) harus satu sumber dengan SK terbaru di naskah, bukan selalu dari SK KGB | `prepareKGBFromForm` + `handleSalaryPreview` + JS preview; test peninjauan dikoreksi (masa KP 8) |
| 2026-09-10 | **Backfill MKG + gaji 4 usulan aktif ke aturan SK pemenang** (110, 119, 121, 124; MKG=EvenYear(masa SK KP)+0/+2; gaji=skala bracket baru; audit `backfill_mkg_20260910` + `backfill_gaji_20260910`; backup `/tmp/sic-backup-submissions-2026.sql`): 110: 6-8→4-6 (3.189.300→3.089.300 / 3.287.000→3.186.600); 119: 24-26→26-28 (5.182.000→5.345.200 / 5.345.200→5.513.600); 121: 16-18→14-16 (4.042.500→3.919.100 / 4.169.900→4.042.500); 124: 22-24→20-22 (4.624.300→4.483.100 / 4.770.000→4.624.300) | Temuan owner: MKG web vs Word beda — akar: data lama tersimpan aturan lama (kode jalur hitung sudah selaras); prinsip KGB +2 th 0 bl dari masa ASN | Tanpa perubahan kode (backfill data + audit saja) |
| 2026-09-10 | **Satu modul penghitung baris masa kerja naskah (`letterdata.HitungMKG`) untuk tiga jalur** (submit/kirim ulang, pratinjau gaji `salary-preview`, pratinjau dasbor `/me`): **poin 6** "Masa kerja golongan pada tgl. tsb" = masa kerja SK pemenang apa adanya; **poin 8** "Berdasarkan Masa Kerja" = **grid genap angka poin 6 ditambah 2 tahun 0 bulan**. Gaji lama/baru dicari pada bracket grid genap poin 6 (+2), sehingga baris masa kerja dan gajinya satu sumber. Fallback masa kerja dari TMT awal (keputusan 2026-09-03) hanya bila masa SK KGB belum tersimpan di master | Temuan owner: ada ketidaksesuaian angka masa kerja antar jalur — pratinjau dasbor memakai `MasaKerjaFromTMT`, pratinjau gaji memakai rumus salinan sendiri, sedangkan submit memakai varian lain; tiga salinan aturan = tiga peluang menyimpang. Prinsip dikunci: KGB selalu berkala 2 tahun, jadi poin 8 tidak ikut membawa bulan SK pemenang, tetapi TETAP mengikuti BESARAN angka poin 6 (KP 26 th dari KGB 24 th → poin 8 = 28, bukan 26; kalau dipatok ke KGB, gaji lama justru turun dan naskah tidak konsisten) | Modul baru `letterdata.HitungMKG` + `SumberMKG`/`MKGLine`; `prepareKGBFromForm`, `handleSalaryPreview`, dan `/me` memanggil modul yang sama; JS `refreshSalary` mengirim `last_kgb_masa_bulan`; `ValidateDraft` memakai invarian `baru = EvenYear(lama) + 2` dengan bulan 0; `MasaKerjaKGB` + `resolveKGBInputs` (jalur lama) dihapus; `MasaKerjaFromTMT` dipertahankan sebagai fallback; test `TestHitungMKGAturanResmi`/`TestHitungMKGInvarianPoin8`/`TestHitungMKGFallbackTanpaSK`/`TestValidateDraftPoin8DariGridPoin6`/`TestValidateDraftPoin8KasusDivergen`/`TestSalaryPreviewPoin8DariGridPoin6`/`TestSalaryPreviewPoin8MasaKPLebihBesar`/`TestSubmitPoin8TerpisahDariSKPemenang` |
| 2026-09-10 | **Sakelar unit tujuan bertipe Dinas ditolak untuk pegawai non-Dinas** (422 di server, bukan hanya UI) | Temuan analisis alur: status awal usulan ditentukan unit master guru, sedangkan unit verifikasi diambil dari unit yang dipilih di form. Pegawai SD yang memilih unit tujuan Dinas akan berstatus `menunggu_unit` padahal unit Dinas tidak punya verifikator unit — usulan tersangkut tanpa jalur keluar | Guard di `prepareKGBFromForm`: `u.Type == "dinas" && t.UnitType != "dinas"` → 422; test `TestSubmitTolakUnitDinasUntukPegawaiNonDinas` |
| 2026-09-10 | **Nomor surat dibersihkan saat penerbitan gagal**: `ReleaseIssue` kini mengosongkan `tte_number` bersama token kunci TTE | Temuan analisis alur: penghitung nomor (`BeginIssue`) ikut menghitung usulan `menunggu_tte` yang `tte_number`-nya terisi, sehingga nomor yang ditinggalkan percobaan gagal membuat urutan melompat pada percobaan berikutnya | `ReleaseIssue` menambah `tte_number=NULL`; tidak ada migrasi |
| 2026-09-11 | **Koreksi penomoran poin 8: ikut BESARAN angka poin 6, hanya bulannya dinolkan.** Setelah deploy aturan 2026-09-10, ditemukan implementasi jangkar (poin 8 dari masa SK KGB) justru menurunkan gaji pada bentuk #119 (KGB 24 th, KP 26 th: poin 8 jadi 26 sehingga gaji lama turun dari bracket 26 ke 24). Dry-run seluruh 11 usulan produksi menunjukkan hanya satu model yang menghindari penurunan gaji: **poin 8 = EvenYear(angka poin 6) + 2 bulan 0**, dengan gaji dari bracket grid yang sama | Prinsip: baris "Masa kerja golongan pada tgl. tsb" dan "Gaji Pokok lama" harus satu turunan SK; pemisahan KP/KGB hanya berlaku pada jangka waktu KGB (TMT), bukan pada angka masa kerja yang dicetak | Modul `HitungMKG` disederhanakan (grid dari angka cetak); field `Draft.MKGKGBTahun` dibatalkan; `ValidateDraft` kembali ke invarian `baru = EvenYear(lama)+2`; test divergen #119/#121/#124 ditambah |
| 2026-09-11 | **Semua SK yang dipersyaratkan WAJIB diunggah — tidak ada pelonggaran fallback berkas utama untuk slot KGB.** Berkas utama skema lama hanya dihitung sebagai pelengkap slot SK KP (dan SK terakhir PPPK), tidak untuk slot KGB/KGB terakhir | Keputusan owner: "harus lengkap semua SK kebutuhan di unggah". Temuan sebelumnya (#108/#109/#110 hanya punya berkas utama) diselesaikan lewat jalur resmi: unit/Dinas tolak dengan catatan, guru kirim ulang dan melengkapi slot yang kurang | `ValidateSubmissionFiles` tetap menuntut slot KGB untuk PNS; UI kirim ulang menandai slot wajib (tanda * + `required`) hanya bila berkas lama belum ada; pesan galat menyebut slot yang kurang. Sertai perbaikan bug: `saveSlotFilesResubmit` mengembalikan daftar berkas BARU saja, sehingga kirim ulang yang gagal menyimpan tidak lagi menghapus berkas lama yang masih dipakai pengajuan berjalan (sebelumnya `removeSlotFiles` dijalankan setelah slot lama disalin) |
| 2026-09-11 | **Tombol Koreksi Data untuk Admin Dinas pada tahap `menunggu_dinas`** (sebelum diteruskan ke pimpinan/TTE). Semua kolom naskah dapat diperbaiki; **kolom penentu gaji (gaji lama/baru atau TMT berlaku) wajib disertai catatan alasan minimal 10 karakter**. Hanya peran `admin_dinas` yang berwenang. Status TIDAK berpindah dan usulan tetap di antrean Dinas. Berkas (SK) tidak diganti lewat jalur ini — berkas keliru tetap lewat mekanisme tolak-kembali. Nilai sebelum dan sesudah koreksi tercatat di `audit_logs` action `koreksi_dinas` beserta daftar kolom yang berubah | Permintaan owner: admin Dinas perlu memperbaiki kesalahan tanpa memaksa guru kirim ulang untuk typo administratif. Ini pengecualian terbatas atas PRD F-7 ("pengajuan terkunci; perbaikan hanya lewat tolak-kembali"): dibatasi peran, tahap, dan wajib beralasan untuk kolom bernilai hak ASN | Endpoint `POST /api/v1/verifications/dinas/{id}/koreksi` (role `admin_dinas`); handler memakai ulang `prepareKGBFromForm` sehingga gaji/poin 6-8 memakai satu mesin hitung; `store.KoreksiDinas` + helper bersama `applyDraftUpdate` (Refactor dari `Resubmit`, hindari duplikasi 50 kolom); UI tombol "Koreksi Data" + form terisi; test `TestKoreksiDinasUbahIdentitas`/`TestKoreksiDinasGajiWajibCatatan`/`TestKoreksiDinasStatusSalah`/`TestKoreksiDinasHanyaAdminDinas` |
| 2026-09-11 | **Kolom Jabatan form usul KGB dapat diisi manual (opsi "Lainnya")**: opsi **"Kepala Sekolah" dihapus** dari dropdown; pilihan rujukan tetap jenjang fungsional guru (Guru Ahli Pertama–Utama) dan daftar jabatan non-guru Disdik (pengawas/penilik/pamong/pelaksana/struktural), ditambah opsi **"Lainnya (ketik manual)"** dengan isian bebas. Server menerima jabatan isian manual selama bentuknya wajar (3–120 karakter, huruf/angka + spasi `. , / ( ) - '`; karakter markup/kendali ditolak) — daftar rujukan jadi pilihan, bukan lagi pembatas keras | Permintaan owner: "hapus jabatan kepala sekolah dan tambah dengan isian lainnya agar jabatan lain bisa mengisi, tidak hanya guru saja, karena di dinas tidak hanya guru ada pengawas penilik dll" | `reference.go`: fungsi baru `validJabatanBebas`; `validJabatanUntuk` menerima isian manual di kedua kategori; `validJabatanNonGuru` kini memakai `validJabatanBebas` sebagai gerbang bentuk (menutup celah "Guru;DROP TABLE" yang lolos lewat pencocokan awalan "GURU"); "Kepala Sekolah" tetap diterima server demi usulan lama. UI `appscript.go`: `JAB` tanpa Kepala Sekolah, select `#naskah-jabatan-pilih` + opsi Lainnya + label isian manual + hidden `name=jabatan`, wiring `syncJabatan()` (required bersyarat, dipanggil di `onsubmit`); CSS `[hidden]{display:none!important}` (menutup override `label{display:block}`). Test `reference_test.go` + `jabatan_manual_test.go` |
